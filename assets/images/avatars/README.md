# Default avatar images

Drop app-provided default avatar images here (e.g. `avatar_1.png`, `avatar_2.svg`).

Once files are added, list their filenames in `kDefaultAvatarAssets`
in `lib/screens/profile_picture_screen.dart` so they show up in the
"Choose a default avatar" picker.
